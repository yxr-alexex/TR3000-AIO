name: TR3000-AIO Build

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-24.04

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Show System Information
        run: |
          echo "================================="
          echo "TR3000-AIO Build Environment"
          echo "================================="
          uname -a
          lscpu
          df -h

      - name: Build Test
        run: |
          echo "Hello TR3000-AIO"
          echo "GitHub Actions is working!"